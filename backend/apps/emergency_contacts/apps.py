from django.apps import AppConfig


class EmergencyContactsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.emergency_contacts'
    label = 'emergency_contacts'
